-- [[ Xx©®7xX xas Mod: Speed Focus (No Distance) ]]
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local LP = Players.LocalPlayer
local Camera = workspace.CurrentCamera

-- سڕینا سکریپتێن کەڤن
if LP.PlayerGui:FindFirstChild("Xx7xX_Xas_SpeedOnly") then LP.PlayerGui:FindFirstChild("Xx7xX_Xas_SpeedOnly"):Destroy() end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "Xx7xX_Xas_SpeedOnly"
ScreenGui.Parent = LP:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false

local Main = Instance.new("Frame")
Main.Parent = ScreenGui
Main.Size = UDim2.new(0, 180, 0, 280)
Main.Position = UDim2.new(0.35, 0, 0.3, 0)
Main.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
Main.BorderSizePixel = 2
Main.BorderColor3 = Color3.fromRGB(255, 255, 0)
Main.Active = true

-- Dragging System
local dragging, dragStart, startPos
Main.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true; dragStart = input.Position; startPos = Main.Position
    end
end)
UIS.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        Main.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
Main.InputEnded:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then dragging = false end end)

local Title = Instance.new("TextLabel")
Title.Parent = Main; Title.Size = UDim2.new(1, 0, 0, 30); Title.Text = "Xx©®7xX xas"; Title.BackgroundColor3 = Color3.fromRGB(40, 40, 0); Title.TextColor3 = Color3.fromRGB(255, 255, 255)

local Input = Instance.new("TextBox")
Input.Parent = Main; Input.PlaceholderText = "ناڤێ یاریزانی..."; Input.Size = UDim2.new(0.9, 0, 0, 30); Input.Position = UDim2.new(0.05, 0, 0.15, 0); Input.BackgroundColor3 = Color3.fromRGB(30, 30, 30); Input.TextColor3 = Color3.fromRGB(255, 255, 255)

local BangBtn = Instance.new("TextButton")
BangBtn.Parent = Main; BangBtn.Text = "BANG: OFF"; BangBtn.Size = UDim2.new(0.9, 0, 0, 35); BangBtn.Position = UDim2.new(0.05, 0, 0.32, 0); BangBtn.BackgroundColor3 = Color3.fromRGB(80, 0, 0); BangBtn.TextColor3 = Color3.fromRGB(255, 255, 255)

local OrbitBtn = Instance.new("TextButton")
OrbitBtn.Parent = Main; OrbitBtn.Text = "ORBIT: OFF"; OrbitBtn.Size = UDim2.new(0.9, 0, 0, 35); OrbitBtn.Position = UDim2.new(0.05, 0, 0.47, 0); OrbitBtn.BackgroundColor3 = Color3.fromRGB(80, 0, 0); OrbitBtn.TextColor3 = Color3.fromRGB(255, 255, 255)

-- Speed Controls
local LabelB = Instance.new("TextLabel")
LabelB.Parent = Main; LabelB.Text = "Bang Speed:"; LabelB.Size = UDim2.new(0.9, 0, 0, 20); LabelB.Position = UDim2.new(0.05, 0, 0.63, 0); LabelB.BackgroundTransparency = 1; LabelB.TextColor3 = Color3.fromRGB(200, 200, 200)

local BangSpeed = Instance.new("TextBox")
BangSpeed.Parent = Main; BangSpeed.Text = "35"; BangSpeed.Size = UDim2.new(0.9, 0, 0, 30); BangSpeed.Position = UDim2.new(0.05, 0, 0.70, 0); BangSpeed.BackgroundColor3 = Color3.fromRGB(25, 25, 25); BangSpeed.TextColor3 = Color3.fromRGB(255, 255, 0)

local OrbitSpeedInput = Instance.new("TextBox")
OrbitSpeedInput.Parent = Main; OrbitSpeedInput.Text = "5"; OrbitSpeedInput.PlaceholderText = "Orbit Speed"; OrbitSpeedInput.Size = UDim2.new(0.9, 0, 0, 30); OrbitSpeedInput.Position = UDim2.new(0.05, 0, 0.85, 0); OrbitSpeedInput.BackgroundColor3 = Color3.fromRGB(25, 25, 25); OrbitSpeedInput.TextColor3 = Color3.fromRGB(0, 255, 0)

local OpenBtn = Instance.new("TextButton")
OpenBtn.Parent = ScreenGui; OpenBtn.Text = "Xx7"; OpenBtn.Size = UDim2.new(0, 45, 0, 45); OpenBtn.Position = UDim2.new(0, 5, 0.5, 0); OpenBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 0); OpenBtn.TextColor3 = Color3.fromRGB(255, 255, 0); OpenBtn.MouseButton1Click:Connect(function() Main.Visible = not Main.Visible end)

-- [[ Logic ]]
local BangActive, OrbitActive, Target = false, false, nil
local BAngle, OAngle = 0, 0

local function GetTarget()
    local text = Input.Text:lower()
    if text == "" then return nil end
    for _, p in pairs(Players:GetPlayers()) do
        if (p.Name:lower():sub(1, #text) == text or p.DisplayName:lower():sub(1, #text) == text) and p ~= LP then return p end
    end
    return nil
end

BangBtn.MouseButton1Click:Connect(function()
    Target = GetTarget()
    if not Target then return end
    BangActive = not BangActive
    BangBtn.Text = BangActive and "BANG: ON" or "BANG: OFF"
    BangBtn.BackgroundColor3 = BangActive and Color3.fromRGB(0, 150, 0) or Color3.fromRGB(80, 0, 0)
    Camera.CameraSubject = (BangActive or OrbitActive) and Target.Character.Humanoid or LP.Character.Humanoid
end)

OrbitBtn.MouseButton1Click:Connect(function()
    Target = GetTarget()
    if not Target then return end
    OrbitActive = not OrbitActive
    OrbitBtn.Text = OrbitActive and "ORBIT: ON" or "ORBIT: OFF"
    OrbitBtn.BackgroundColor3 = OrbitActive and Color3.fromRGB(0, 150, 0) or Color3.fromRGB(80, 0, 0)
    Camera.CameraSubject = (BangActive or OrbitActive) and Target.Character.Humanoid or LP.Character.Humanoid
end)

RunService.Heartbeat:Connect(function(dt)
    if (BangActive or OrbitActive) and Target and Target.Character and Target.Character:FindFirstChild("HumanoidRootPart") then
        local MyRoot = LP.Character:FindFirstChild("HumanoidRootPart")
        local T_Root = Target.Character.HumanoidRootPart
        if MyRoot then
            local bSpd = tonumber(BangSpeed.Text) or 35
            local oSpd = tonumber(OrbitSpeedInput.Text) or 5
            local dist = 1.5 -- مەسافەیا جێگیر
            
            BAngle = BAngle + (bSpd * dt)
            OAngle = OAngle + (oSpd * dt)
            
            local Motion = BangActive and math.sin(BAngle) * 0.75 or 0
            
            if OrbitActive then
                local cosX = math.cos(OAngle) * (dist + Motion)
                local sinZ = math.sin(OAngle) * (dist + Motion)
                MyRoot.CFrame = CFrame.new(T_Root.Position + Vector3.new(cosX, 0, sinZ), T_Root.Position)
            else
                MyRoot.CFrame = T_Root.CFrame * CFrame.new(0, 0, dist + Motion)
            end
        end
    end
end)
